/** Demonstrates how the FailureSequence class works. */
public class FailureSequenceDemo {
    public static void main(String[] args) {
        /*FailureSequence fs = new FailureSequence();

        StudentArrayDeque<Integer> sad = new StudentArrayDeque<Integer>();
        ArrayDequeSolution<Integer> ads = new ArrayDequeSolution<Integer>();

        DequeOperation dequeOp1 = new DequeOperation("addFirst", 5);
        fs.addOperation(dequeOp1);
        sad.addFirst(5);
        ads.addFirst(5);
        assertEquals(fs.toString(), sad.get(0), ads.get(0));

        DequeOperation dequeOp2 = new DequeOperation("addFirst", 10);
        fs.addOperation(dequeOp2);
        sad.addFirst(10);
        ads.addFirst(10);
        assertEquals(fs.toString(), ads.get(1), sad.get(1));

        DequeOperation dequeOp3 = new DequeOperation("size");
        fs.addOperation(dequeOp3);
        assertEquals(fs.toString(), ads.size(), sad.size());

        DequeOperation dequeOp4 = new DequeOperation("removeFirst");
        fs.addOperation(dequeOp4);
        assertEquals(fs.toString(), ads.removeFirst(), sad.removeFirst());

        DequeOperation dequeOp5 = new DequeOperation("removeLast");
        fs.addOperation(dequeOp5);
        assertEquals(fs.toString(), ads.removeLast(), sad.removeLast());

        DequeOperation dequeOp6 = new DequeOperation("size");
        fs.addOperation(dequeOp6);
        assertEquals(fs.toString(), ads.size(), sad.size());

        DequeOperation dequeOp7 = new DequeOperation("removeLast");
        fs.addOperation(dequeOp7);
        assertEquals(fs.toString(), ads.removeLast(), sad.removeLast());

        DequeOperation dequeOp8 = new DequeOperation("size");
        fs.addOperation(dequeOp8);
        assertEquals(fs.toString(), ads.size(), sad.size());

        DequeOperation dequeOp9 = new DequeOperation("addFirst", 1);
        fs.addOperation(dequeOp9);
        sad.addFirst(1);
        ads.addFirst(1);
        assertEquals(fs.toString(), ads.get(0), sad.get(0));
*/
    }
} 
